{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "comment": "Watch patterns are relative to this service's root directory (frontend), so '**' here means 'any change under frontend/'. A backend-only change touches nothing in this path and does not rebuild the portal.",
    "watchPatterns": ["**"]
  },
  "deploy": {
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1
  }
}
